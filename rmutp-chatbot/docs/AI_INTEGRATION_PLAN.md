# แผนการเพิ่ม AI เข้าสู่ระบบ RMUTP Chatbot

อัปเดต: 15 มกราคม 2026

---

## สารบัญ

1. [ภาพรวมและวัตถุประสงค์](#1-ภาพรวมและวัตถุประสงค์)
2. [โครงสร้างเดิมที่ต้องเก็บไว้](#2-โครงสร้างเดิมที่ต้องเก็บไว้)
3. [ส่วนที่ควรเพิ่ม AI เข้ามา](#3-ส่วนที่ควรเพิ่ม-ai-เข้ามา)
4. [สถาปัตยกรรมแบบ Hybrid](#4-สถาปัตยกรรมแบบ-hybrid)
5. [ทางเลือก AI ที่เหมาะสม](#5-ทางเลือก-ai-ที่เหมาะสม)
6. [ขั้นตอนการพัฒนา](#6-ขั้นตอนการพัฒนา)
7. [ประโยชน์ที่คาดว่าจะได้รับ](#7-ประโยชน์ที่คาดว่าจะได้รับ)
8. [ข้อควรระวัง](#8-ข้อควรระวัง)

---

## 1. ภาพรวมและวัตถุประสงค์

### สถานะปัจจุบัน
ระบบ RMUTP Chatbot เป็นระบบ **Rule-based** ที่ทำงานด้วยหลักการ:
- Pattern Matching (ตรวจจับคำสำคัญ)
- Keyword Search
- Fuzzy Matching
- Synonym Expansion

### วัตถุประสงค์การเพิ่ม AI
1. **เพิ่มความเข้าใจภาษาธรรมชาติ**: ตอบคำถามที่ซับซ้อนขึ้น หลากหลายรูปแบบ
2. **ตอบคำถามที่ไม่มีใน FAQ**: สร้างคำตอบจากบริบทหลายแหล่ง
3. **เรียนรู้จากการใช้งาน**: ปรับปรุงความแม่นยำอัตโนมัติ
4. **แยกความตั้งใจ (Intent)**: เข้าใจเจตนาผู้ใช้ได้ดีขึ้น
5. **สนทนาต่อเนื่อง**: จำบริบทการสนทนาก่อนหน้า

---

## 2. โครงสร้างเดิมที่ต้องเก็บไว้

> ส่วนเหล่านี้ไม่เกี่ยวกับ AI แต่เป็นโครงสร้างพื้นฐานที่จำเป็น

### 2.1 ฐานข้อมูล (Database)
```
ต้องเก็บไว้ 100%
```

**เหตุผล**: 
- เป็นแหล่งข้อมูลความจริง (Source of Truth)
- ข้อมูลมีโครงสร้างชัด สามารถเชื่อถือได้
- AI จะใช้ข้อมูลเหล่านี้เป็น Context

**ตารางที่ต้องเก็บ**:
- `faqs`: 81 รายการ FAQ
- `staff`: 118 อาจารย์
- `departments`: 10 สาขา
- `news`: ข่าวสารล่าสุด
- `chat_logs`: ประวัติการสนทนา (สำหรับเทรน AI)
- `admin_users`: ผู้ดูแลระบบ

**ข้อมูลเพิ่มเติมที่ควรเก็บ**:
- `user_feedback`: การให้คะแนน/ฟีดแบคคำตอบ
- `intent_logs`: บันทึก Intent ที่ระบบตรวจจับได้

### 2.2 ระบบ Backend พื้นฐาน
```
ต้องเก็บไว้: db.php, security.php, admin_login.php
```

**เหตุผล**:
- **db.php**: การเชื่อมต่อฐานข้อมูล (AI ก็ยังต้องใช้)
- **security.php**: CORS, Rate Limiting, Input Validation
- **admin_login.php**: ระบบจัดการผู้ดูแล

### 2.3 Admin Dashboard
```
ต้องเก็บไว้ 100%
```

**เหตุผล**:
- เจ้าหน้าที่ต้องจัดการ FAQ, Staff, ข่าวสาร
- ตรวจสอบ Chat Logs
- **เพิ่มเติม**: ควรมี Dashboard สำหรับดู AI Performance

**ไฟล์**:
- `admin/dashboard.html`
- `admin/login.html`
- `backend/admin_api.php`

### 2.4 ระบบ Scraping
```
ต้องเก็บไว้
```

**เหตุผล**:
- ดึงข่าวสารอัตโนมัติ
- AI จะใช้ข้อมูลนี้เป็น Context ในการตอบคำถาม

**ไฟล์**:
- `scripts/scrape_news.php`
- `scripts/run_scraper.bat`
- `scripts/setup_scheduler.ps1`

### 2.5 Frontend (UI)
```
ต้องเก็บไว้ แต่ปรับปรุง
```

**เหตุผล**:
- หน้าเว็บที่ผู้ใช้โต้ตอบกับ Chatbot
- ต้องเพิ่มความสามารถสำหรับรองรับ AI

**ต้องเพิ่ม**:
- แสดงว่าคำตอบมาจาก Rule หรือ AI
- แสดง Confidence Score
- ปุ่ม Feedback (ชอบ/ไม่ชอบคำตอบ)
- Loading indicator สำหรับ AI (อาจช้ากว่า Rule)

---

## 3. ส่วนที่ควรเพิ่ม AI เข้ามา

### 3.1 NLP Engine (ใหม่)
```
ทำงาน: เข้าใจคำถามภาษาไทยลึกซึ้งขึ้น
```

**ฟีเจอร์**:
- **Intent Classification**: จำแนกเจตนา (ถามอาจารย์, ค่าเทอม, กู้เงิน, ฯลฯ)
- **Entity Recognition (NER)**: ดึงชื่อ, สาขา, วันที่, จำนวนเงิน
- **Semantic Understanding**: เข้าใจความหมายเชิงบริบท
- **Context Management**: จำบริบทการสนทนา

**ตัวอย่าง**:
```
ผู้ใช้: "สาขาคอมมีกี่ห้องแลป"
Intent: ถามสิ่งอำนวยความสะดวก
Entity: {department: "computer", facility: "lab"}
Context: ไม่มีบริบทก่อนหน้า

ผู้ใช้: "แล้วมีกี่ตัวครับ"  [ต่อจากข้อก่อน]
Intent: ถามจำนวน
Entity: จับจากบริบท
Context: กำลังพูดถึงสาขาคอม
```

### 3.2 Response Generation (ใหม่)
```
ทำงาน: สร้างคำตอบที่เป็นธรรมชาติ
```

**วิธีการ**:
- **Template-based**: ใช้เทมเพลตกับข้อมูลจากฐานข้อมูล (ควบคุมได้)
- **AI Generation**: ใช้ LLM สร้างคำตอบจาก Context (ยืดหยุ่น)

**ตัวอย่าง**:
```
ข้อมูลจาก DB: 
- สาขาคอมพิวเตอร์มี 3 ห้องแล็ป
- ห้องแล็บ 1: 40 เครื่อง
- ห้องแล็บ 2: 50 เครื่อง
- ห้องแล็บ 3: 30 เครื่อง

AI สร้างคำตอบ:
"สาขาวิศวกรรมคอมพิวเตอร์มีห้องปฏิบัติการคอมพิวเตอร์ทั้งหมด 3 ห้อง 
รวมเครื่องคอมพิวเตอร์ 120 เครื่องครับ แบ่งเป็น ห้องแล็บ 1 มี 40 เครื่อง 
ห้องแล็บ 2 มี 50 เครื่อง และห้องแล็บ 3 มี 30 เครื่องครับ"
```

### 3.3 Hybrid Decision Engine (ใหม่)
```
ทำงาน: ตัดสินใจว่าจะใช้ Rule หรือ AI
```

**Logic**:
```
1. ถ้าคำถามตรง FAQ (Exact Match) → ใช้ Rule (รวดเร็ว, แม่นยำ 100%)
2. ถ้าคำถามซับซ้อน/ไม่มีใน FAQ → ส่งให้ AI
3. ถ้า AI Confidence < 70% → ส่งต่อเจ้าหน้าที่
4. ถ้าคำถามหลายส่วน → แยกเป็นคำถามย่อย แล้วรวม
```

**ตัวอย่าง**:
```
คำถาม: "อาจารย์สาขาคอมคนไหนสอนเรื่อง AI"
→ Hybrid: ค้นหา Rule (อาจารย์สาขาคอม) + AI (ดู expertise)

คำถาม: "ถ้าจะสมัครเรียนสาขาคอมต้องเตรียมตัวยังไง"
→ AI Pure: สร้างคำตอบจาก FAQ หลายรายการ + ข่าวสาร
```

### 3.4 Learning & Feedback System (ใหม่)
```
ทำงาน: เรียนรู้จากการใช้งาน
```

**ฟีเจอร์**:
- บันทึก User Feedback (👍👎)
- วิเคราะห์คำถามที่ตอบไม่ได้
- ปรับปรุง Intent Model
- แนะนำ FAQ ใหม่ที่ควรเพิ่ม

### 3.5 Vector Database (ใหม่ - ถ้าใช้ RAG)
```
ทำงาน: เก็บ Embeddings สำหรับ Semantic Search
```

**ข้อมูลที่เก็บ**:
- FAQ Embeddings
- News Embeddings
- Staff Expertise Embeddings

**เปรียบเทียบ**:
| วิธี | ข้อดี | ข้อเสีย |
|------|-------|---------|
| **SQL Full-text Search** | เร็ว, ง่าย | จับความหมายไม่ได้ |
| **Vector Search** | เข้าใจความหมาย | ช้ากว่า, ซับซ้อน |

**ตัวอย่าง**:
```
คำถาม: "ค่าใช้จ่ายต่อเทอม"
SQL: ไม่เจอ (ใน FAQ ใช้คำว่า "ค่าเทอม")
Vector: เจอ! (เข้าใจว่าเป็นคำพ้อง)
```

---

## 4. สถาปัตยกรรมแบบ Hybrid

### 4.1 โครงสร้างระบบใหม่

```
                   ┌──────────────┐
                   │   Frontend   │
                   │  (index.html)│
                   └──────┬───────┘
                          │
                          ▼
                  ┌──────────────────┐
                  │   API Gateway    │
                  │  (chatbot.php)   │
                  └────────┬─────────┘
                          │
            ┌─────────────┴─────────────┐
            ▼                           ▼
    ┌───────────────┐          ┌──────────────┐
    │  Rule Engine  │          │  AI Engine   │
    │  (เดิม)       │          │  (ใหม่)      │
    └───────┬───────┘          └──────┬───────┘
            │                         │
            │     ┌───────────────────┤
            │     │                   │
            ▼     ▼                   ▼
    ┌────────────────┐     ┌──────────────────┐
    │   MySQL DB     │     │  Vector DB       │
    │  (Staff/FAQ)   │     │  (Embeddings)    │
    └────────────────┘     └──────────────────┘
```

### 4.2 Decision Flow

```
คำถามเข้ามา
    │
    ▼
┌─────────────────────┐
│ Pre-processing      │
│ (Cleaning, Tokenize)│
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Intent Detection    │ ◄── AI Model
└──────────┬──────────┘
           │
           ▼
    ┌─────┴─────┐
    │  Decision  │
    └─────┬─────┘
          │
    ┌─────┴──────┐
    │            │
    ▼            ▼
Rule-based    AI-based
    │            │
    │   ┌────────┴────────┐
    │   │                 │
    │   ▼                 ▼
    │ RAG              LLM
    │ (ค้นหา Vector)   (สร้างคำตอบ)
    │   │                 │
    └───┴─────────────────┘
            │
            ▼
    ┌──────────────┐
    │ Confidence   │
    │ Check        │
    └──────┬───────┘
           │
      ┌────┴────┐
   High        Low
      │         │
      ▼         ▼
   ตอบ      ส่งต่อเจ้าหน้าที่
```

### 4.3 ตัวอย่างการทำงาน

#### สถานการณ์ 1: คำถามตรง FAQ
```
Input: "ค่าเทอมเท่าไหร่"

1. Intent: ถามค่าเทอม (Confidence 95%)
2. Decision: ใช้ Rule (เพราะมีใน FAQ)
3. Query: SELECT * FROM faqs WHERE category = 'tuition'
4. Response: ตามข้อมูล FAQ
5. Time: ~50ms
```

#### สถานการณ์ 2: คำถามซับซ้อน
```
Input: "ถ้าจะสมัครเรียนสาขาคอมแต่ไม่มีเงินจะทำยังไง"

1. Intent: หลายเจตนา (รับสมัคร + กู้เงิน)
2. Decision: ใช้ AI (คำถามซับซ้อน)
3. RAG Process:
   - ค้นหา FAQ เกี่ยวกับ TCAS (Vector Similarity)
   - ค้นหา FAQ เกี่ยวกับกยศ. (Vector Similarity)
   - ดึงข่าวล่าสุดเกี่ยวกับทุนการศึกษา
4. LLM Generation:
   - รับ Context จาก RAG
   - สร้างคำตอบที่เชื่อมโยง
5. Response: คำตอบแบบเรียงความ
6. Time: ~2000ms
```

#### สถานการณ์ 3: สนทนาต่อเนื่อง
```
คำถาม 1: "สาขาคอมเรียนอะไรบ้าง"
AI: บริบท = "กำลังสนใจสาขาคอมพิวเตอร์"
Response: รายวิชาต่างๆ

คำถาม 2: "แล้วยากไหม"
AI: ดึงบริบทก่อนหน้า → เข้าใจว่าถาม "รายวิชาสาขาคอมยากไหม"
Response: ตอบตามบริบท
```

---

## 5. ทางเลือก AI ที่เหมาะสม

### 5.1 แนวทางที่ 1: Rule-based + Simple ML (แนะนำสำหรับเริ่มต้น)

**เทคโนโลยี**:
- Intent Classification: Naive Bayes / SVM
- NER: pythainlp + Regex
- Similarity: TF-IDF + Cosine Similarity

**ข้อดี**:
- ง่ายต่อการพัฒนา
- ไม่ต้องการ GPU
- ควบคุมผลลัพธ์ได้
- ทำงานบน XAMPP/PHP ได้

**ข้อเสีย**:
- ความยืดหยุ่นจำกัด
- ต้องเทรนใหม่เมื่อมี Intent เพิ่ม

**เหมาะกับ**:
- Budget จำกัด
- ข้อมูลเทรนน้อย
- ต้องการผลลัพธ์ที่ควบคุมได้

### 5.2 แนวทางที่ 2: RAG (Retrieval-Augmented Generation)

**เทคโนโลยี**:
- Embedding Model: OpenAI Embeddings / Thai BERT
- Vector DB: Pinecone / Chroma / FAISS
- LLM: GPT-4, GPT-3.5, Claude

**ข้อดี**:
- ตอบคำถามที่ไม่มีใน FAQ
- สร้างคำตอบที่เป็นธรรมชาติ
- ใช้ข้อมูลล่าสุดได้

**ข้อเสีย**:
- ต้องเชื่อมต่อ API (มีค่าใช้จ่าย)
- ช้ากว่า Rule-based
- ควบคุมผลลัพธ์ยากกว่า

**เหมาะกับ**:
- ต้องการคุณภาพสูง
- มี Budget API
- ยอมรับความไม่แน่นอน

### 5.3 แนวทางที่ 3: Local LLM (Open Source)

**เทคโนโลยี**:
- Model: Llama 3, Typhoon Thai LLM, SeaLLM
- Runtime: Ollama, LM Studio
- Framework: LangChain

**ข้อดี**:
- ไม่มีค่า API
- ข้อมูลไม่ออกจากเซิร์ฟเวอร์ (Privacy)
- ปรับแต่งได้เต็มที่

**ข้อเสีย**:
- ต้องการ GPU (แรง)
- Setup ซับซ้อน
- คุณภาพอาจต่ำกว่า GPT-4

**เหมาะกับ**:
- มีเซิร์ฟเวอร์แรง
- ข้อมูลละเอียดอ่อน
- Long-term ไม่อยากจ่ายค่า API

### 5.4 ตารางเปรียบเทียบ

| แนวทาง | ค่าใช้จ่าย | ความแม่นยำ | ความเร็ว | ความยาก | แนะนำ |
|--------|-----------|-----------|----------|---------|--------|
| **Rule + Simple ML** | ต่ำ | 70-80% | เร็วมาก | ง่าย | ⭐⭐⭐⭐⭐ เริ่มต้น |
| **RAG + GPT API** | กลาง | 85-95% | กลาง | กลาง | ⭐⭐⭐⭐ ถ้ามี Budget |
| **Local LLM** | สูง (ฮาร์ดแวร์) | 80-90% | ช้า | ยาก | ⭐⭐⭐ ถ้ามีทีมแข็ง |
| **Hybrid All** | กลาง-สูง | 90-95% | ดี | ยาก | ⭐⭐⭐⭐⭐ Production |

---

## 6. ขั้นตอนการพัฒนา

### Phase 1: Foundation (2-4 สัปดาห์)

**เป้าหมาย**: เตรียมข้อมูลและโครงสร้างพื้นฐาน

1. **Data Preparation**
   - รวบรวม chat logs ที่มี
   - สร้างชุด Intent Labels (ถามอาจารย์, ค่าเทอม, กู้เงิน, ฯลฯ)
   - Annotate ข้อมูล 100-200 ตัวอย่าง

2. **Infrastructure**
   - ติดตั้ง Python (สำหรับ ML)
   - Setup Virtual Environment
   - ติดตั้ง pythainlp, scikit-learn

3. **Database Extension**
   - สร้างตาราง `intents`
   - สร้างตาราง `user_feedback`
   - สร้างตาราง `ai_performance_logs`

### Phase 2: Basic AI (4-6 สัปดาห์)

**เป้าหมาย**: สร้าง Intent Classifier แรก

1. **Intent Classification Model**
   - เทรน Naive Bayes / SVM
   - ทดสอบความแม่นยำ (เป้า >75%)
   - บันทึก Model

2. **API Integration**
   - สร้าง Python API (Flask/FastAPI)
   - เชื่อมต่อกับ chatbot.php
   - ทดสอบการเรียกใช้

3. **Hybrid Logic**
   - เขียน Decision Engine
   - กำหนด Threshold
   - ทดสอบทั้ง Rule + AI

### Phase 3: Advanced Features (6-8 สัปดาห์)

**เป้าหมาย**: เพิ่ม RAG หรือ LLM

1. **Embedding & Vector Search**
   - สร้าง Embeddings จาก FAQ
   - ติดตั้ง Vector DB (เช่น Chroma)
   - Semantic Search

2. **Response Generation**
   - เชื่อมต่อ LLM API (ถ้าใช้ GPT)
   - หรือ Setup Local LLM (ถ้าใช้ Ollama)
   - Template Design

3. **Context Management**
   - Session Management
   - บันทึกบริบทการสนทนา

### Phase 4: Learning & Optimization (ต่อเนื่อง)

**เป้าหมาย**: เรียนรู้และปรับปรุง

1. **Feedback Loop**
   - ให้ผู้ใช้ให้คะแนนคำตอบ
   - วิเคราะห์คำถามที่ตอบไม่ได้
   - Fine-tune Model

2. **Performance Monitoring**
   - Dashboard ดู AI Metrics
   - A/B Testing (Rule vs AI)
   - Optimize Response Time

3. **Continuous Improvement**
   - เพิ่ม FAQ จากคำถามบ่อย
   - ปรับ Threshold
   - อัปเดต Model

---

## 7. ประโยชน์ที่คาดว่าจะได้รับ

### 7.1 ประสิทธิภาพ

| ตัวชี้วัด | ก่อนใช้ AI | หลังใช้ AI | ปรับปรุง |
|----------|-----------|-----------|---------|
| **ตอบได้ถูก** | 70% | 85-90% | +15-20% |
| **เข้าใจคำถาม** | Simple | Complex | +++ |
| **ตอบคำถามนอก FAQ** | 0% | 60-70% | +60% |
| **เวลาตอบเฉลี่ย** | 50ms | 200-500ms | ช้าลง แต่แม่น |
| **User Satisfaction** | 3.5/5 | 4.2/5 | +0.7 |

### 7.2 ประสบการณ์ผู้ใช้

**ก่อนใช้ AI**:
```
ผู้ใช้: "ถ้าผมจะสมัครเรียนต้องทำยังไง"
Bot: "ขออภัยครับ ไม่พบคำตอบที่เกี่ยวข้อง"

ผู้ใช้: "สมัครเรียน TCAS"
Bot: [แสดง FAQ เกี่ยวกับ TCAS]
```

**หลังใช้ AI**:
```
ผู้ใช้: "ถ้าผมจะสมัครเรียนต้องทำยังไง"
Bot: "สำหรับการสมัครเรียน คุณสามารถสมัครผ่านระบบ TCAS ได้ 5 รอบครับ 
     ดูรายละเอียดได้ที่ [FAQ TCAS] หรือถ้าต้องการสอบถามเพิ่มเติม 
     สามารถติดต่อสาขาที่สนใจได้โดยตรงครับ"
```

### 7.3 ลดภาระงาน

- **คำถามที่ตอบเองได้**: 70% → 85%
- **คำถามที่ต้องส่งต่อเจ้าหน้าที่**: 30% → 15%
- **ประหยัดเวลาเจ้าหน้าที่**: 40-50%

---

## 8. ข้อควรระวัง

### 8.1 ข้อจำกัดของ AI

1. **Hallucination**: AI อาจสร้างข้อมูลเท็จ
   - **แก้**: ใช้ RAG (ดึงข้อมูลจากฐานข้อมูล)
   - **แก้**: ตั้ง Confidence Threshold

2. **ต้นทุน**: API มีค่าใช้จ่าย
   - **แก้**: ใช้ Hybrid (Rule ก่อน, AI ตามหลัง)
   - **แก้**: Cache คำตอบที่ซ้ำ

3. **ความเร็ว**: AI ช้ากว่า Rule
   - **แก้**: Async Processing
   - **แก้**: แสดง Typing Indicator

4. **Privacy**: ข้อมูลผู้ใช้ส่งไป API
   - **แก้**: ใช้ Local LLM
   - **แก้**: Anonymize ข้อมูล

### 8.2 Best Practices

1. **ทดสอบก่อนใช้งานจริง**
   - Unit Test
   - Integration Test
   - User Acceptance Test

2. **เริ่มจากง่าย**
   - Phase 1: Intent Classification
   - Phase 2: Simple RAG
   - Phase 3: Full LLM

3. **มี Fallback**
   - ถ้า AI ล้ม → Rule-based
   - ถ้า Rule ล้ม → บอกผู้ใช้ตรงๆ

4. **Monitor อย่างต่อเนื่อง**
   - Log ทุกคำตอบ
   - วัด Accuracy
   - เก็บ Feedback

### 8.3 เงื่อนไขความสำเร็จ

1. **ข้อมูลเพียงพอ**: อย่างน้อย 500-1000 chat logs
2. **ทีมพร้อม**: มีคนที่เข้าใจทั้ง PHP และ Python
3. **Budget**: API หรือ GPU
4. **ความอดทน**: AI ไม่สมบูรณ์ตั้งแต่วันแรก ต้อง Fine-tune

---

## สรุป

### โครงสร้างเดิมที่ไม่เกี่ยวกับ AI (เก็บไว้ 100%)

1. ✅ **ฐานข้อมูล** (MySQL): Staff, FAQ, News, Departments
2. ✅ **Backend พื้นฐาน**: db.php, security.php, admin_login.php
3. ✅ **Admin Dashboard**: จัดการข้อมูล, ดู Logs
4. ✅ **Scraping System**: ดึงข่าวสารอัตโนมัติ
5. ✅ **Frontend UI**: หน้าเว็บแชท (ปรับปรุงเล็กน้อย)

### ส่วนที่เพิ่ม AI เข้ามา

1. 🤖 **NLP Engine**: Intent, NER, Context
2. 🤖 **Response Generator**: สร้างคำตอบอัจฉริยะ
3. 🤖 **Hybrid Decision**: เลือก Rule หรือ AI
4. 🤖 **Learning System**: เรียนรู้จาก Feedback
5. 🤖 **Vector Database**: Semantic Search (ถ้าใช้ RAG)

### แนวทางที่แนะนำ

**ขั้นที่ 1 (เดือนที่ 1-2)**: Rule + Simple ML
- เริ่มจาก Intent Classification
- ควบคุมได้, ง่าย, ประหยัด

**ขั้นที่ 2 (เดือนที่ 3-4)**: เพิ่ม RAG
- ถ้าผลลัพธ์ดีและมี Budget
- เชื่อมต่อ GPT API

**ขั้นที่ 3 (เดือนที่ 5+)**: Optimize
- Fine-tune จาก User Feedback
- A/B Testing
- Scale

---

## ไฟล์ที่เกี่ยวข้อง

- [README.md](../README.md): ภาพรวมระบบ
- [THEORY.md](THEORY.md): ทฤษฎีพื้นฐาน
- [PROJECT_STATUS_CURRENT.md](PROJECT_STATUS_CURRENT.md): สถานะปัจจุบัน
- [NEXT_STEPS.md](../NEXT_STEPS.md): ขั้นตอนถัดไป

---

**หมายเหตุ**: เอกสารนี้เป็นแนวคิดและการวางแผน ยังไม่ได้เขียน Code
หากต้องการเริ่มพัฒนาจริง ควรเริ่มจาก Phase 1 และทดสอบทีละขั้นตอน
