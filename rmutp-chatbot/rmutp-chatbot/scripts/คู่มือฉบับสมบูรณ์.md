# คู่มือระบบดึงข่าวอัตโนมัติ

สำหรับ: RMUTP Engineering Chatbot
ภาษา: PHP (ใช้ XAMPP ได้เลย)
อัปเดต: 14 มกราคม 2026

---

## สารบัญ

1. ระบบคืออะไร
2. เริ่มใช้งาน (3 ขั้นตอน)
3. เช็คสถานะและดูแล
4. แก้ปัญหา
5. คำสั่งที่ใช้บ่อย

---

# 1. ระบบคืออะไร

News Scraper = ตัวดึงข่าวอัตโนมัติ

หน้าที่:
- ดึงข่าวจาก https://eng.rmutp.ac.th/
- เก็บลงฐานข้อมูล MySQL (ตาราง news)
- ให้ Chatbot นำไปตอบผู้ใช้
- รันอัตโนมัติ 4 ครั้ง/วัน (00:00, 06:00, 12:00, 18:00)

ทำอะไรได้:
- ตรวจสอบข่าวซ้ำ (ไม่เพิ่มซ้ำ)
- แยกหมวดหมู่ (ข่าวประชาสัมพันธ์ / กิจกรรม)
- สกัด Tags คำสำคัญ
- ลบข่าวเก่า > 180 วัน
- บันทึก Log รายวัน
- ใช้ PHP (ไม่ต้องติดตั้ง Python)

การทำงาน:

1. Windows Task Scheduler (รันทุก 6 ชั่วโมง)
   ↓
2. run_scraper.bat (เรียก PHP)
   ↓
3. scrape_news.php
   - เข้าเว็บ eng.rmutp.ac.th
   - ดึงข่าวทั้งหมด (200+ ข่าว)
   - เช็คข่าวซ้ำ
   - เพิ่มข่าวใหม่เข้า MySQL
   - ลบข่าวเก่า > 180 วัน
   - บันทึก log
   ↓
4. MySQL Database (eng_chatbot) → ตาราง news
   ↓
5. Chatbot (backend/chatbot.php) → ตอบคำถามเกี่ยวกับข่าว

ไฟล์ที่เกี่ยวข้อง:

scripts/
├── scrape_news.php          [หัวใจของระบบ - ตัวดึงข่าวหลัก]
├── run_scraper.bat          [รัน manual - คลิกได้เลย]
├── setup_scheduler.ps1      [ติดตั้ง Task Scheduler - ต้อง Admin]
├── check_logs.ps1           [เช็ค log อัตโนมัติ]
├── คู่มือฉบับสมบูรณ์.md      [ไฟล์นี้]
└── logs/                    [เก็บ log รายวัน]

---

# 📌 ส่วนที่ 2: การติดตั้งและใช้งาน

## ขั้นตอนที่ 1: เช็คความพร้อม

### ✅ สิ่งที่ต้องมี

1. **XAMPP** ติดตั้งแล้ว (มี Apache + MySQL + PHP)
2. **ฐานข้อมูล** `eng_chatbot` มีอยู่แล้ว
3. **ตาราง** `news` ถูกสร้างแล้ว
4. **Windows 10/11** (สำหรับ Task Scheduler)

### 🔍 วิธีเช็ค

```powershell
# 1. เช็ค PHP
C:\xampp\php\php.exe -v

# 2. เช็ค MySQL
cd C:\xampp\mysql\bin
.\mysql.exe -u root -e "SHOW DATABASES;"

# 3. เช็คตาราง news
.\mysql.exe -u root eng_chatbot -e "SHOW TABLES LIKE 'news';"

# 4. เช็คโฟลเดอร์
Test-Path "C:\xampp\htdocs\rmutp-chatbot\scripts"
```

โครงสร้างตาราง news:

```sql
CREATE TABLE IF NOT EXISTS news (
  2. เริ่มใช้งาน (3 ขั้นตอน)

## ขั้นตอนที่ 1: เช็คความพร้อม

ต้องมี:
1. XAMPP ติดตั้งแล้ว (Apache + MySQL + PHP)
2. ฐานข้อมูล eng_chatbot
3. ตาราง news
4. Windows 10/11

วิธีเช็ค:

---

## ขั้นตอนที่ 2: ทดสอบรันครั้งแรก

### 🚀 วิธีที่ 1: Double-Click (ง่ายที่สุด!)

1. เปิด Windows Explorer
2. ไปที่ `C:\xampp\htdocs\rmutp-chatbot\scripts`
3. **Double-click** ไฟล์ `run_scraper.bat`
3 วิธีรัน:

วิธีที่ 1 (ง่ายสุด): 
- Double-click ไฟล์ run_scraper.bat

วิธีที่ 2:
```powershell
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\run_scraper.bat
```

วิธีที่ 3:
```powershell
C:\xampp\php\php.exe scrape_news.php
```

ผลลัพธ์ที่ควรเห็น:=====================================
📅 2026-01-14 23:45:00

✅ เชื่อมต่อฐานข้อมูลสำเร็จ

🔍 กำลังดึงข่าวจาก https://eng.rmutp.ac.th/ ...
📰 พบ links ทั้งหมด: 204 รายการ
RMUTP Engineering News Scraper
เชื่อมต่อฐานข้อมูลสำเร็จ
กำลังดึงข่าวจาก https://eng.rmutp.ac.th/
พบ links ทั้งหมด: 204 รายการ

ข่าวซ้ำ: ประชาสัมพันธ์การรับสมัคร...
ข่าวซ้ำ: กิจกรรมวันครู...

เพิ่มข่าวใหม่: 2 รายการ
ข่าวซ้ำ: 30 รายการ

ทำความสะอาดข่าวเก่า: ปิดการแสดง 0 รายการ

สถิติข่าวในระบบ:
- ข่าวประชาสัมพันธ์: 14 รายการ
- กิจกรรม: 19 รายการ
- รวม: 33 รายการ

ใช้เวลา: 12.45 วินาที
Log file: scripts/logs/scraper_2026-01-14.log
```

ทดสอบว่าข่าวเข้าฐานข้อมูล:
| total |
+-------+
|    33 |
+-------+
```

---

## ขั้นตอนที่ 3: ตั้งค่ารันอัตโนมัติ

### ⚙️ ติดตั้ง Windows Task Scheduler

**⚠️ สำคัญ: ต้องรันด้วยสิทธิ์ Administrator!**

#### 1. เปิด PowerShell แบบ Admin

- กด `Win + X`
- เลือก **"Terminal (Admin)"** หรือ **"PowerShell (Admin)"**
---

## ขั้นตอนที่ 3: ตั้งค่ารันอัตโนมัติ

สำคัญ: ต้องรันด้วยสิทธิ์ Administrator

เปิด PowerShell แบบ Admin:
- กด Win + X
- เลือก Terminal (Admin) หรือ PowerShell (Admin)
- กด Yes ถ้ามี UAC ถาม

รันคำสั่งติดตั้ง:12:00, 18:00
  - รันโดย: SYSTEM account
  - รันแม้ไม่ login: Yes

============================================================
✅ ติดตั้งสำเร็จ!

ทดสอบรันทันที:
  Start-ScheduledTask -TaskName "RMUTP News Scraper"

ดูสถานะ:
  Get-ScheduledTask -TaskName "RMUTP News Scraper"
============================================================
```

ผลลัพธ์ที่ควรเห็น:

```
RMUTP News Scraper - Task Scheduler Setup

กำลังสร้าง Scheduled Task...
สร้าง Task สำเร็จ: RMUTP News Scraper

การตั้งค่า:
- ชื่อ Task: RMUTP News Scraper
- ความถี่: 4 ครั้งต่อวัน
- เวลา: 00:00, 06:00, 12:00, 18:00
- รันโดย: SYSTEM account
- รันแม้ไม่ login: Yes

ติดตั้งสำเร็จ!
```

ทดสอบ Task:
### 1️⃣ เช็ค Task Scheduler

```powershell
Get-ScheduledTask -TaskName "*RMUTP*"
```

ควรเห็น:
```
TaskPath  TaskName            State
--------  --------            -----
\         RMUTP News Scraper  Ready
```

ดูรายละเอียด:

```powershell
Start-ScheduledTask -TaskName "RMUTP News Scraper"

# เช็คสถานะ
Get-ScheduledTaskInfo -TaskName "RMUTP News Scraper"
```

ตารางการรัน (4 ครั้ง/วัน ทุก 6 ชั่วโมง):
- 00:00 - เที่ยงคืน (รับข่าววันใหม่)
- 06:00 - เช้า (ก่อนคนเข้าทำงาน)
- 12:00 - เที่ยง (ช่วงกลางวัน)
- 18:00 - เย็น (ก่อนเลิกงานร็จ |
| `Disabled` | ❌ ถูกปิดใช้งาน | รัน `Enable-ScheduledTask` |

### ความหมายของ LastTaskResult

| Code | ความหมาย |
|------|----------|
| `0` | ✅ สำเร็จ 100% |
| `2147946720` | ⚠️ Warning เล็กน้อย (ยังใช้งานได้) |
| `267009` | ❌ Task ยังไม่เคยรัน |
| อื่นๆ | ❌ เกิด error (เช็ค log) |

---

## ดู Log Files

วิธีที่ 1: ใช้ Script อัตโนมัติ (แนะนำ)

```powershell
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\check_logs.ps1
```

ได้อะไรบ้าง:
- แสดงไฟล์ log ทั้งหมด
- บอกอายุของแต่ละ log (เช่น "2h 30m ago")
- วิเคราะห์ย้อนหลัง 7 วัน (หรือระบุเอง)
- นับจำนวนครั้งที่รัน + ข่าวใหม่ + ข่าวซ้ำ
- เตือนถ้ามีวันที่ไม่มี log
- แสดง log ล่าสุด

ตัวอย่าง Output:

```
News Scraper Log Checker

[OK] Found 5 log files

Log Files:

Date       Size   LastModified        Age
----       ----   ------------        ---
2026-01-14 0.4 KB 2026-01-14 23:24:44 0h 15m ago
2026-01-14 0.5 KB 2026-01-14 18:00:12 5h 39m ago
2026-01-14 0.4 KB 2026-01-14 12:00:08 11h 39m ago
2026-01-11 0.4 KB 2026-01-11 23:47:36 2d 23h ago
2026-01-02 0.9 KB 2026-01-02 23:43:58 11d 23h ago

============================================================
  Summary (Last 7 days)
============================================================

Daily Report:

  2026-01-14
     - Runs: 3 times
     - New news: 2 items
     - Duplicates: 28 items

  2026-01-11
     - Runs: 1 times
     - New news: 1 items
     - Duplicates: 31 items

============================================================
  Overall Summary (7 days)
============================================================

  - Days with logs:    2 / 7 days
  - Total runs:        4 times
  - New news:          3 items
  - Duplicates:        59 items
  - Average/day:       1 runs

[WARNING] Missing 5 days of logs
   Task Scheduler might not be working

============================================================
  Latest Log (first 15 lines)
============================================================

File: scraper_2026-01-14.log
Time: 2026-01-14 23:24:44

   [2026-01-14 17:24:44] [INFO] เชื่อมต่อฐานข้อมูลสำเร็จ
   [2026-01-14 17:24:44] [INFO] เริ่มดึงข่าวจาก https://eng.rmutp.ac.th/
   [2026-01-14 17:24:44] [INFO] เพิ่มข่าว: คณะวิทยาศาสตร์...
```

ดู Log ช่วงเวลาที่ต้องการ:

```powershell
.\check_logs.ps1 -Days 1   # วันนี้
.\check_logs.ps1           # 7 วัน (default)
.\check_logs.ps1 -Days 14  # 14 วัน
.\check_logs.ps1 -Days 30  # 30 วัน
```

อ่านผลลัพธ์:
- Days with logs: 7/7 = ครบทุกวัน (ปกติ)
- Days with logs: 2/7 = มีแค่ 2 วัน (Task Scheduler อาจไม่ทำงาน)
- Total runs: 28 times = รัน 28 ครั้งใน 7 วัน (ปกติ 4 ครั้ง/วัน)
- Total runs: 2 times = รันแค่ 2 ครั้ง (ส่วนใหญ่รัน manual)
- Age: 0h 15m ago = Log อายุ 15 นาที (เพิ่งรัน)
- Age: 2d 23h ago = Log อายุเกือบ 3 วัน (ไม่มีการรัน)

---

### 📂 วิธีที่ 2: ดู Log แบบ Manual

**ตำแหน่งไฟล์:**
```
C:\xampp\htdocs\rmutp-chatbot\scripts\logs\
```

**รูปแบบชื่อไฟล์:**
```
scraper_YYYY-MM-DD.log

ตัวอย่าง:
- scraper_2026-01-14.log  (วันนี้)
- scraper_2026-01-13.log  (เมื่อวาน)
- scraper_2026-01-11.log  (3 วันก่อน)
```

### คำสั่งดู Log

```powershell
cd C:\xampp\htdocs\rmutp-chatbot\scripts\logs

# ⚠️ สำคัญ: ต้องใช้ -Encoding UTF8 เพื่อแสดงภาษาไทย!

# ดู 20 บรรทัดล่าสุด
Get-Content scraper_*.log -Encoding UTF8 | Select-Object -Last 20

# ดู log วันนี้ทั้งหมด
Get-Content scraper_$(Get-Date -Format "yyyy-MM-dd").log -Encoding UTF8

# ดู log วันที่ 14 มกราคม
Get-Content scraper_2026-01-14.log -Encoding UTF8

# หา error ใน log
Get-Content scraper_*.log -Encoding UTF8 | Select-String "ERROR"

# หา warning ใน log
Get-Content scraper_*.log -Encoding UTF8 | Select-String "WARNING"

# นับจำนวน error
(Get-Content scraper_*.log -Encoding UTF8 | Select-String "ERROR").Count
```

### 🎨 เข้าใจ Log Format

```
[2026-01-14 17:24:44] [INFO] เชื่อมต่อฐานข้อมูลสำเร็จ
│                     │      │
│                     │      └─ ข้อความ
│                     └─ ระดับความสำคัญ
└─ วันที่และเวลา
```

| ระดับ | ความหมาย |
|-------|----------|
| `[INFO]` | ✅ ข้อมูลปกติ |
| `[WARNING]` | ⚠️ คำเตือน (ควรสังเกต) |
| `[ERROR]` | ❌ ข้อผิดพลาด (ต้องแก้) |

---

## เช็คข่าวในฐานข้อมูล

### จำนวนข่าวทั้งหมด

```powershell
cd C:\xampp\mysql\bin
.\mysql.exe -u root eng_chatbot -e "SELECT COUNT(*) as 'ข่าวทั้งหมด' FROM news WHERE is_active=1;"
```

### แยกตามหมวดหมู่

```powershell
.\mysql.exe -u root eng_chatbot -e "SELECT category as 'หมวดหมู่', COUNT(*) as 'จำนวน' FROM news WHERE is_active=1 GROUP BY category;"
```

**ตัวอย่างผลลัพธ์:**
```
+----------------+----------+
| หมวดหมู่        | จำนวน    |
+----------------+----------+
| ข่าว           | 14       |
| กิจกรรม        | 19       |
+----------------+----------+
รวม: 33 รายการ
```

### ดูข่าวล่าสุด 10 รายการ

```powershell
.\mysql.exe -u root eng_chatbot -e "SELECT id, LEFT(title, 50) as title, category, DATE(created_at) as date FROM news WHERE is_active=1 ORDER BY id DESC LIMIT 10;"
```

### เช็คว่าข่าวล่าสุดเก่าแค่ไหน

```powershell
.\mysql.exe -u root eng_chatbot -e "SELECT MAX(created_at) as 'ข่าวล่าสุด', DATEDIFF(NOW(), MAX(created_at)) as 'เก่ากว่าวันนี้กี่วัน' FROM news WHERE is_active=1;"
```

**ตัวอย่าง:**
```
+---------------------+------------------+
| ข่าวล่าสุด           | เก่ากว่าวันนี้กี่วัน |
+---------------------+------------------+
| 2026-01-14 17:24:44 |                0 |
+---------------------+------------------+
```

**หมายความว่า:** ข่าวล่าสุดเป็นวันนี้ (0 วัน) = ✅ Scraper ทำงานปกติ

---

# 📌 ส่วนที่ 4: แก้ปัญหา

## ปัญหาที่พบบ่อย

### ❌ ปัญหา 1: Task Scheduler ไม่รัน

**อาการ:**
- ตรวจสอบ log แล้วมีแค่ 1-2 วันใน 7 วัน
- `check_logs.ps1` แสดง "Missing X days of logs"
- ข่าวในฐานข้อมูลเก่ามาก

**เช็คสาเหตุ:**

```powershell
# 1. ดูว่ามี Task หรือไม่
Get-ScheduledTask -TaskName "*RMUTP*"

# 2. ดูสถานะ Task
$info = Get-ScheduledTaskInfo -TaskName "RMUTP News Scraper"
Write-Host "รันครั้งล่าสุด: $($info.LastRunTime)"
Write-Host "รันครั้งถัดไป: $($info.NextRunTime)"
```

**วิธีแก้:**

```powershell
# ถ้าไม่มี Task - ติดตั้งใหม่ (ต้อง Admin)
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\setup_scheduler.ps1

# ถ้า State = Disabled - เปิดใช้งาน
Enable-ScheduledTask -TaskName "RMUTP News Scraper"

# ถ้ามี Task แต่ไม่รัน - ลองรัน manual
Start-ScheduledTask -TaskName "RMUTP News Scraper"
```

---

### ❌ ปัญหา 2: ไม่มี Log Files

**อาการ:**
- `check_logs.ps1` บอก "Found 0 log files"
- โฟลเดอร์ `logs/` ไม่มี หรือว่างเปล่า

**สาเหตุ:**
1. Scraper ไม่เคยรัน
2. โฟลเดอร์ `logs/` ถูกลบ
3. ไม่มี permission เขียนไฟล์

**วิธีแก้:**

```powershell
# เช็คว่าโฟลเดอร์มีหรือไม่
Test-Path "C:\xampp\htdocs\rmutp-chatbot\scripts\logs"

# ถ้าไม่มี - สร้างใหม่
New-Item -ItemType Directory -Force -Path "C:\xampp\htdocs\rmutp-chatbot\scripts\logs"

# รัน scraper manual ทดสอบ
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\run_scraper.bat

# เช็คว่ามีไฟล์ log เกิดขึ้นไหม
Get-ChildItem logs\scraper_*.log
```

---

### ❌ ปัญหา 3: MySQL Connection Error

**อาการ:**
```
[ERROR] ไม่สามารถเชื่อมต่อฐานข้อมูล
```

**สาเหตุ:**
- MySQL ไม่ได้เปิด
- Username/Password ผิด
- Database `eng_chatbot` ไม่มี

**วิธีแก้:**

```powershell
# 1. เช็คว่า MySQL รันอยู่หรือไม่
Get-Process mysqld -ErrorAction SilentlyContinue

# 2. ถ้าไม่มี - เปิด XAMPP Control Panel
#    แล้วกด Start ที่ MySQL

# 3. ทดสอบ connection
cd C:\xampp\mysql\bin
.\mysql.exe -u root -e "SHOW DATABASES;"

# 4. เช็คว่ามี eng_chatbot หรือไม่
.\mysql.exe -u root -e "SHOW DATABASES LIKE 'eng_chatbot';"
```

---

### ❌ ปัญหา 4: ดึงข่าวไม่ได้ (0 ข่าวใหม่)

**อาการ:**
```
✅ เพิ่มข่าวใหม่: 0 รายการ
⏭️  ข่าวซ้ำ: 32 รายการ
```

**สาเหตุที่เป็นไปได้:**
1. ไม่มีข่าวใหม่จริงๆ (ปกติ)
2. เว็บไซต์เปลี่ยน HTML structure
3. Internet connection มีปัญหา

**วิธีแก้:**

```powershell
# 1. ทดสอบเปิดเว็บไซต์
Start-Process "https://eng.rmutp.ac.th/"

# 2. เช็ค log หา error
cd C:\xampp\htdocs\rmutp-chatbot\scripts\logs
Get-Content scraper_*.log -Encoding UTF8 | Select-String "ERROR"

# 3. ดูว่าข่าวล่าสุดเก่าแค่ไหน
cd C:\xampp\mysql\bin
.\mysql.exe -u root eng_chatbot -e "SELECT MAX(created_at), DATEDIFF(NOW(), MAX(created_at)) as วันที่ผ่านมา FROM news;"
```

**ถ้าข่าวล่าสุดเก่า > 7 วัน:** แสดงว่ามีปัญหาจริง
**ถ้าข่าวล่าสุดเป็นวันนี้:** แสดงว่าไม่มีข่าวใหม่ (ปกติ)

---

### ❌ ปัญหา 5: PHP Error

**อาการ:**
```
'php.exe' is not recognized as an internal or external command
```

**สาเหตุ:**
- XAMPP ไม่ได้ติดตั้ง
- Path ของ PHP ผิด

**วิธีแก้:**

```powershell
# เช็คว่า PHP มีหรือไม่
C:\xampp\php\php.exe -v

# ควรเห็น:
# PHP 8.x.x (cli) ...
```

**ถ้าไม่มี:**
1. ติดตั้ง XAMPP จาก https://www.apachefriends.org/
2. แก้ไข path ใน `run_scraper.bat` ให้ถูกต้อง

---

## แก้ปัญหาภาษาไทย

### ❌ อาการ: เห็นอักขระแบบนี้

```
เน€เธเธทเนเธญเธกเธ•เนเธญเธเธฒเธเธเนเธญเธกเธนเธฅ
```

### ✅ สาเหตุ

PowerShell อ่าน log ด้วย **default encoding** (ไม่ใช่ UTF-8)
แต่ log files บันทึกเป็น **UTF-8** (สำหรับภาษาไทย)

### 💡 วิธีแก้

**เพิ่ม `-Encoding UTF8` ทุกครั้งที่ใช้ `Get-Content`!**

### เปรียบเทียบ

```powershell
# ❌ ผิด - ภาษาไทยแสดงเป็นเครื่องหมายแปลกๆ
Get-Content logs\scraper_*.log | Select-Object -Last 20

# ✅ ถูก - ภาษาไทยแสดงถูกต้อง
Get-Content logs\scraper_*.log -Encoding UTF8 | Select-Object -Last 20
```

### คำสั่งที่ถูกต้อง (จำไว้เลย)

```powershell
# ดู log ล่าสุด
Get-Content logs\scraper_*.log -Encoding UTF8 | Select-Object -Last 20

# ดู log ทั้งหมด
Get-Content logs\scraper_2026-01-14.log -Encoding UTF8

# หา error
Get-Content logs\scraper_*.log -Encoding UTF8 | Select-String "ERROR"

# ดู real-time
Get-Content logs\scraper_*.log -Encoding UTF8 -Wait
```

---

# 📌 ส่วนที่ 5: อ้างอิง

## คำสั่งที่ใช้บ่อย

### 🚀 การรัน Scraper

```powershell
# วิธีที่ 1: ใช้ batch file
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\run_scraper.bat

# วิธีที่ 2: รัน PHP โดยตรง
C:\xampp\php\php.exe scrape_news.php

# วิธีที่ 3: Force run Task Scheduler
Start-ScheduledTask -TaskName "RMUTP News Scraper"
```

### 📊 ตรวจสอบสถานะ

```powershell
# เช็ค Task Scheduler
Get-ScheduledTask -TaskName "*RMUTP*" | Select-Object TaskName, State

# เช็ค log
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\check_logs.ps1

# เช็คข่าวในฐานข้อมูล
cd C:\xampp\mysql\bin
.\mysql.exe -u root eng_chatbot -e "SELECT COUNT(*) FROM news WHERE is_active=1;"
```

### 📝 ดู Logs

```powershell
cd C:\xampp\htdocs\rmutp-chatbot\scripts\logs

# ดู 20 บรรทัดล่าสุด
Get-Content scraper_*.log -Encoding UTF8 | Select-Object -Last 20

# หา error
Get-Content scraper_*.log -Encoding UTF8 | Select-String "ERROR"

# ดู log วันนี้
Get-Content scraper_$(Get-Date -Format "yyyy-MM-dd").log -Encoding UTF8
```

### 🔧 จัดการ Task Scheduler

```powershell
# ดูสถานะเต็ม
$info = Get-ScheduledTaskInfo -TaskName "RMUTP News Scraper"
Write-Host "รันล่าสุด: $($info.LastRunTime)"
Write-Host "รันต่อไป: $($info.NextRunTime)"
Write-Host "ผลลัพธ์: $($info.LastTaskResult)"

# รัน task ทันที
Start-ScheduledTask -TaskName "RMUTP News Scraper"

# เปิดใช้งาน task (ถ้าถูกปิด)
Enable-ScheduledTask -TaskName "RMUTP News Scraper"

# ลบ task
Unregister-ScheduledTask -TaskName "RMUTP News Scraper" -Confirm:$false
```

### 💾 คำสั่ง MySQL

```powershell
cd C:\xampp\mysql\bin

# จำนวนข่าวทั้งหมด
.\mysql.exe -u root eng_chatbot -e "SELECT COUNT(*) as total FROM news WHERE is_active=1;"

# แยกตามหมวดหมู่
.\mysql.exe -u root eng_chatbot -e "SELECT category, COUNT(*) FROM news WHERE is_active=1 GROUP BY category;"

# ข่าวล่าสุด 10 รายการ
.\mysql.exe -u root eng_chatbot -e "SELECT id, title, DATE(created_at) FROM news WHERE is_active=1 ORDER BY id DESC LIMIT 10;"

# เช็คว่าข่าวล่าสุดเก่าแค่ไหน
.\mysql.exe -u root eng_chatbot -e "SELECT MAX(created_at) as latest, DATEDIFF(NOW(), MAX(created_at)) as days_old FROM news;"
```

---

## การปรับแต่งระบบ

### 🔧 เปลี่ยนความถี่รัน Scraper

**ปัจจุบัน:** 4 ครั้ง/วัน (00:00, 06:00, 12:00, 18:00)

**ต้องการเปลี่ยน?** แก้ไขไฟล์ `setup_scheduler.ps1`:

```powershell
# เพิ่มเวลา
$trigger5 = New-ScheduledTaskTrigger -Daily -At "04:00"
$trigger6 = New-ScheduledTaskTrigger -Daily -At "08:00"

# แล้วเพิ่ม trigger ใน Register-ScheduledTask
-Trigger @($trigger1, $trigger2, $trigger3, $trigger4, $trigger5, $trigger6)
```

หลังแก้ไข ต้องรัน:
```powershell
# ลบ task เก่า
Unregister-ScheduledTask -TaskName "RMUTP News Scraper" -Confirm:$false

# ติดตั้งใหม่ (ต้อง Admin)
.\setup_scheduler.ps1
```

---

### 🗑️ เปลี่ยนระยะเวลาเก็บข่าว

**ปัจจุบัน:** เก็บย้อนหลัง 180 วัน (6 เดือน)

**ต้องการเปลี่ยน?** แก้ไขไฟล์ `scrape_news.php` (บรรทัดที่ ~346):

```php
// ปัจจุบัน
$scraper->cleanupOldNews(180);

// เปลี่ยนเป็น 1 ปี
$scraper->cleanupOldNews(365);

// เปลี่ยนเป็น 3 เดือน
$scraper->cleanupOldNews(90);

// เปลี่ยนเป็น 30 วัน
$scraper->cleanupOldNews(30);
```

---

### 📊 ดูสถิติการทำงาน

```powershell
# ดูว่ารันไปกี่ครั้งใน 7 วัน
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\check_logs.ps1 -Days 7

# ดูว่ารันไปกี่ครั้งในเดือนนี้
.\check_logs.ps1 -Days 30

# ดูข่าวที่เพิ่มในแต่ละวัน
cd C:\xampp\mysql\bin
.\mysql.exe -u root eng_chatbot -e "SELECT DATE(created_at) as date, COUNT(*) as count FROM news WHERE is_active=1 GROUP BY DATE(created_at) ORDER BY date DESC LIMIT 7;"
```

---

## 💡 Tips & Best Practices

### ✅ ควรทำ (Do)

- ✅ **เช็ค log เป็นประจำ** - อย่างน้อยสัปดาห์ละครั้ง
  ```powershell
  .\check_logs.ps1
  ```

- ✅ **ใช้ `-Encoding UTF8`** ทุกครั้งที่อ่าน log
  ```powershell
  Get-Content logs\scraper_*.log -Encoding UTF8
  ```

- ✅ **เช็ค Task Scheduler** ว่ายัง active อยู่
  ```powershell
  Get-ScheduledTask -TaskName "*RMUTP*"
  ```

- ✅ **ทดสอบรัน manual** ก่อนปล่อยอัตโนมัติ
  ```powershell
  .\run_scraper.bat
  ```

- ✅ **สำรองฐานข้อมูล** เป็นประจำ
  ```powershell
  cd C:\xampp\mysql\bin
  .\mysqldump.exe -u root eng_chatbot news > news_backup_$(Get-Date -Format "yyyy-MM-dd").sql
  ```

### ❌ ไม่ควรทำ (Don't)

- ❌ **อย่าลบ log files ทั้งหมด** - เก็บไว้อย่างน้อย 30 วัน
- ❌ **อย่าปิด XAMPP** ตอนที่ task กำลังรัน
- ❌ **อย่าแก้ Task Scheduler** ถ้าไม่แน่ใจ
- ❌ **อย่าลืมเช็ค LastTaskResult** หลังจาก task รัน
- ❌ **อย่าลบข่าวในฐานข้อมูล** โดยไม่สำรอง

---

## 📈 สถิติที่ควรสังเกต

### ✅ สถานะปกติ

| Metric | ค่าที่คาดหวัง |
|--------|---------------|
| **Days with logs** | 7/7 days (100%) |
| **Total runs** | ~28 times (4x/day × 7 days) |
| **New news** | 5-15 items/week |
| **Average/day** | 4 runs |
| **Latest log age** | < 6 hours |

### ⚠️ สถานะผิดปกติ

| อาการ | สาเหตุที่เป็นไปได้ | แก้ไข |
|-------|-------------------|-------|
| Days < 7/7 | Task Scheduler ไม่ทำงาน | ติดตั้ง Task ใหม่ |
| Total runs = 0-2 | Manual run อย่างเดียว | เช็ค Task Scheduler |
| New news = 0 (7 วันติด) | Scraper error หรือเว็บเปลี่ยน | เช็ค log หา error |
| Latest log age > 24h | ไม่มีการรันเลย | รัน manual ด่วน |
| LastTaskResult ≠ 0 | Task run แต่มี error | เช็ค log |

---

## 🎓 เข้าใจระบบให้ลึกขึ้น

### ทำไมต้องรัน 4 ครั้ง/วัน?

- **00:00** - รับข่าวที่ประกาศตอนดึก
- **06:00** - รับข่าวเช้า (ก่อนคนเข้าทำงาน)
- **12:00** - รับข่าวช่วงกลางวัน
- **18:00** - รับข่าวเย็น (ก่อนเลิกงาน)

รวม = **ข่าวทันสมัยตลอดทั้งวัน**

### ทำไมต้องเก็บข่าวแค่ 180 วัน?

- ข่าวเก่า > 6 เดือน = **ไม่เกี่ยวข้องแล้ว**
- ฐานข้อมูลไม่บวม = **ค้นหาเร็วขึ้น**
- ยังคงเก็บไว้ในฐานข้อมูล (is_active=0) = **สำรองไว้**

### ทำไมใช้ PHP ไม่ใช่ Python?

| Python | PHP |
|--------|-----|
| ❌ ต้องติดตั้งเพิ่ม | ✅ **XAMPP มีอยู่แล้ว** |
| ❌ ต้องติดตั้ง libraries | ✅ **ใช้ได้ทันที** |
| ❌ Virtual environment ยุ่งยาก | ✅ **ไม่มีปัญหา** |
| ⭐⭐⭐ ความยาก | ⭐ ความยาก |

---

## 📞 สรุปท้ายคู่มือ

### 🚀 Quick Start (เริ่มใช้งานเลย!)

```powershell
# 1. ทดสอบรันครั้งแรก
cd C:\xampp\htdocs\rmutp-chatbot\scripts
.\run_scraper.bat

# 2. ตั้งค่ารันอัตโนมัติ (ต้อง Admin)
.\setup_scheduler.ps1

# 3. เช็คสถานะ
.\check_logs.ps1
```

### 📊 คำสั่ง 3 อันดับที่ต้องจำ

```powershell
# 1️⃣ เช็ค log (ควรทำทุกวัน)
.\check_logs.ps1

# 2️⃣ เช็ค Task Scheduler
Get-ScheduledTask -TaskName "*RMUTP*"

# 3️⃣ รัน manual (กรณีฉุกเฉิน)
.\run_scraper.bat
```

### 🆘 เมื่อเจอปัญหา

1. **เช็ค log ก่อน** → `.\check_logs.ps1`
2. **ดูข้อผิดพลาด** → `Get-Content logs\scraper_*.log -Encoding UTF8 | Select-String "ERROR"`
3. **รัน manual ทดสอบ** → `.\run_scraper.bat`
4. **เช็ค Task Scheduler** → `Get-ScheduledTask -TaskName "*RMUTP*"`
5. **อ่านส่วนแก้ปัญหา** → หน้า 16-20 ของคู่มือนี้

---

## 🌟 เวอร์ชันและการอัปเดต

**เวอร์ชัน:** 2.0 (รวมทุกคู่มือไว้ในไฟล์เดียว)  
**อัปเดตล่าสุด:** 14 มกราคม 2026  
**ผู้พัฒนา:** RMUTP Chatbot Development Team

### ประวัติการเปลี่ยนแปลง

- **v2.0** (14 ม.ค. 2026)
  - รวม 3 คู่มือเป็นไฟล์เดียว
  - เพิ่มคำอธิบายตั้งแต่พื้นฐาน
  - เพิ่ม `check_logs.ps1` script
  - เขียนใหม่ให้อ่านง่ายขึ้น

- **v1.0** (12 ม.ค. 2026)
  - เปลี่ยนจาก Python เป็น PHP
  - เพิ่ม Task Scheduler setup
  - เพิ่มระบบ log รายวัน

---

**📖 จบคู่มือ - พร้อมใช้งานแล้ว! 🚀**

---

## 📎 ภาคผนวก

### ไฟล์ที่ถูกรวมเข้ามา

คู่มือฉบับนี้รวมเนื้อหาจาก:

1. **README.md** - ภาพรวมและวิธีใช้งานพื้นฐาน
2. **USAGE_GUIDE.md** - การเช็คระบบและแก้ปัญหา
3. **LOG_MONITORING.md** - การใช้ check_logs.ps1

**ไฟล์เดิมยังมีอยู่** - คู่มือนี้เป็นการรวมให้อ่านง่ายขึ้นเท่านั้น

### ติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- เช็ค log files: `scripts/logs/scraper_*.log`
- อ่านส่วนแก้ปัญหาในคู่มือนี้
- ทดสอบรัน manual: `.\run_scraper.bat`
