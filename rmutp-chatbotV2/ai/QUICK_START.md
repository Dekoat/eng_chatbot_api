# 🚀 Quick Start - เริ่มใช้งาน AI ใน 5 นาที

สำหรับคนที่รีบ ไม่อยากอ่านเยอะ

---

## ขั้นตอนที่ 1: ติดตั้ง Python (ถ้ายังไม่มี)

1. ดาวน์โหลด: https://www.python.org/downloads/
2. ติดตั้ง โดย **ติ๊กถูก "Add Python to PATH"**
3. เสร็จแล้วรีสตาร์ท Terminal

---

## ขั้นตอนที่ 2: ติดตั้งระบบ AI (ครั้งเดียว)

### แบบง่าย: ใช้ Batch File

**ดับเบิลคลิกที่:**
```
setup_ai.bat
```

รอประมาณ **2-5 นาที** จนเห็น:
```
✅ ติดตั้งระบบ AI สำเร็จ!
```

### แบบพิมพ์คำสั่ง:

```powershell
cd c:\xampp\htdocs\rmutp-chatbot
pip install -r ai\api\requirements.txt
python ai\scripts\train_model.py
```

---

## ขั้นตอนที่ 3: รัน API Server

**ดับเบิลคลิกที่:**
```
run_api_server.bat
```

เห็นข้อความนี้ = สำเร็จ:
```
✅ Model loaded successfully
* Running on http://127.0.0.1:5000
```

⚠️ **อย่าปิดหน้าต่างนี้**

---

## ขั้นตอนที่ 4: ทดสอบ (ตัวเลือก)

### ทดสอบ Model

**ดับเบิลคลิกที่:**
```
run_test_model.bat
```

ลองพิมพ์:
- "ค่าเทอมเท่าไหร่"
- "อาจารย์สาขาคอม"
- "quit" (เพื่อออก)

### ทดสอบ PHP Integration

**เปิด Terminal ใหม่** แล้วดับเบิลคลิก:
```
run_test_php.bat
```

เห็น:
```
✅ AI API is available
Intent: ask_tuition
```

= สำเร็จ!

---

## ขั้นตอนที่ 5: เชื่อมต่อกับ Chatbot (ต่อไป)

ดูรายละเอียดใน: [GETTING_STARTED.md](GETTING_STARTED.md)

---

## Troubleshooting เร็ว

### ❌ "python is not recognized"
→ ติดตั้ง Python ใหม่ + เลือก "Add to PATH"

### ❌ "Model not found"
→ รัน `setup_ai.bat` อีกครั้ง

### ❌ "Port 5000 already in use"
→ ปิดโปรแกรมที่ใช้ Port 5000 หรือรีสตาร์ทเครื่อง

### ❌ "pip install ล้มเหลว"
→ รันด้วย Administrator หรือใช้:
```powershell
pip install --user -r ai\api\requirements.txt
```

---

## ไฟล์ที่ใช้บ่อย

| ไฟล์ | คำอธิบาย |
|------|----------|
| `setup_ai.bat` | ติดตั้งครั้งแรก (รันครั้งเดียว) |
| `run_api_server.bat` | รัน API (ทุกครั้งที่ใช้งาน) |
| `run_test_model.bat` | ทดสอบ Model |
| `run_test_php.bat` | ทดสอบ PHP Integration |
| `run_train_model.bat` | เทรน Model ใหม่ |

---

## เสร็จแล้ว! 🎉

1. ✅ API รันอยู่ที่ `http://localhost:5000`
2. ✅ PHP เรียก API ได้
3. ✅ พร้อมเชื่อมต่อกับ Chatbot

**ขั้นตอนถัดไป:** ดู [GETTING_STARTED.md](GETTING_STARTED.md) เพื่ออ่านรายละเอียดเพิ่มเติม
