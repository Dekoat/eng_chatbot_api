<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RMUTP Engineering Chatbot</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Sarabun:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --accent-maroon: #940032;
            --accent-maroon-hover: #7a0028;
            --accent-gold: #D4AF37;
            --accent-blue: #003B71;
            --bg-primary: #FFFFFF;
            --bg-secondary: #F7F7F8;
            --bg-tertiary: #ECECF1;
            --border-light: #E5E5E5;
            --text-primary: #2D2D2D;
            --text-secondary: #6B6B6B;
            --text-tertiary: #ACACAC;
            --shadow-subtle: 0 1px 2px rgba(0, 0, 0, 0.04);
            --shadow-soft: 0 2px 8px rgba(0, 0, 0, 0.06);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Sarabun', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            line-height: 1.6;
            height: 100vh;
            overflow: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* Layout */
        .app-container {
            display: flex;
            height: 100vh;
            max-width: 1600px;
            margin: 0 auto;
        }

        /* Minimal Sidebar */
        .sidebar {
            width: 260px;
            background: var(--bg-primary);
            border-right: 1px solid var(--border-light);
            display: flex;
            flex-direction: column;
            padding: 16px 12px;
        }

        .sidebar-header {
            padding: 12px;
            margin-bottom: 16px;
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 16px;
            padding: 14px 16px;
            margin-bottom: 20px;
            background: linear-gradient(145deg, #fff, var(--bg-secondary));
            border-radius: 16px;
            border: 1px solid rgba(148, 0, 50, 0.08);
            box-shadow: 0 8px 20px rgba(20, 20, 20, 0.05);
        }

        .logo-image {
            width: 54px;
            height: 54px;
            object-fit: contain;
            filter: drop-shadow(0 4px 8px rgba(148, 0, 50, 0.25));
            flex-shrink: 0;
        }

        .logo-text {
            display: flex;
            flex-direction: column;
            gap: 2px;
            flex: 1;
            min-width: 0;
        }

        .logo-title {
            font-size: 16px;
            font-weight: 600;
            color: var(--accent-maroon);
            line-height: 1.15;
            letter-spacing: -0.01em;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .logo-subtitle {
            font-size: 12px;
            color: var(--text-secondary);
            line-height: 1.15;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .new-chat-btn {
            width: 100%;
            background: var(--bg-primary);
            border: 1px solid var(--border-light);
            color: var(--text-primary);
            padding: 10px 14px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .new-chat-btn:hover {
            background: var(--bg-secondary);
            border-color: var(--accent-maroon);
        }

        .new-chat-btn svg {
            width: 18px;
            height: 18px;
            stroke: var(--text-secondary);
        }

        .sidebar-nav {
            flex: 1;
            overflow-y: auto;
            margin-top: 16px;
        }

        .nav-section {
            margin-bottom: 24px;
        }

        .nav-section-title {
            font-size: 11px;
            font-weight: 600;
            color: var(--text-tertiary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            padding: 0 12px 8px;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 10px 12px;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.15s ease;
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 14px;
            margin-bottom: 2px;
        }

        .nav-item:hover {
            background: var(--bg-secondary);
            color: var(--text-primary);
        }

        .nav-item.active {
            background: var(--bg-secondary);
            color: var(--text-primary);
            font-weight: 500;
        }

        .nav-icon {
            font-size: 16px;
            width: 20px;
            text-align: center;
            opacity: 0.5;
            color: var(--text-secondary);
        }

        .nav-item:hover .nav-icon {
            opacity: 0.7;
        }

        .nav-item.active .nav-icon {
            opacity: 1;
            color: var(--accent-maroon);
        }

        /* SVG Icons */
        .icon-svg {
            width: 18px;
            height: 18px;
            stroke: currentColor;
            fill: none;
            stroke-width: 2;
            stroke-linecap: round;
            stroke-linejoin: round;
        }

        .sidebar-footer {
            border-top: 1px solid var(--border-light);
            padding-top: 12px;
            margin-top: 12px;
        }

        /* Main Chat Area */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            background: var(--bg-primary);
            position: relative;
        }

        /* Top Bar */
        .top-bar {
            height: 56px;
            border-bottom: 1px solid var(--border-light);
            display: flex;
            align-items: center;
            padding: 0 24px;
            background: var(--bg-primary);
        }

        .top-bar-title {
            font-size: 14px;
            font-weight: 500;
            color: var(--text-secondary);
        }

        .top-bar-actions {
            margin-left: auto;
            display: flex;
            gap: 8px;
        }

        .icon-btn {
            width: 32px;
            height: 32px;
            border: none;
            background: transparent;
            color: var(--text-secondary);
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.15s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
        }

        .icon-btn:hover {
            background: var(--bg-secondary);
            color: var(--text-primary);
        }

        /* Messages Container */
        .messages-container {
            flex: 1;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        .messages-wrapper {
            max-width: 800px;
            margin: 0 auto;
            padding: 32px 24px;
        }

        /* Message */
        .message {
            display: flex;
            gap: 16px;
            margin-bottom: 32px;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(8px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .message-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            flex-shrink: 0;
            background: var(--bg-tertiary);
        }

        .message.bot .message-avatar {
            background: linear-gradient(135deg, var(--accent-maroon) 0%, #7a0028 100%);
            color: white;
        }

        .message.user .message-avatar {
            background: var(--bg-tertiary);
            color: var(--text-secondary);
        }

        .message-avatar svg {
            width: 18px;
            height: 18px;
        }

        .message-content {
            flex: 1;
            padding-top: 6px;
        }

        .message-text {
            font-size: 15px;
            line-height: 1.7;
            color: var(--text-primary);
            word-wrap: break-word;
        }

        .message-meta {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-top: 8px;
            font-size: 12px;
            color: var(--text-tertiary);
        }

        .confidence-badge {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            background: rgba(76, 175, 80, 0.1);
            color: #2e7d32;
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 500;
        }

        .confidence-badge.low {
            background: rgba(255, 152, 0, 0.1);
            color: #e65100;
        }

        .sources-info {
            margin-top: 12px;
            padding: 12px 16px;
            background: var(--bg-secondary);
            border-radius: 8px;
            font-size: 13px;
            color: var(--text-secondary);
            border-left: 3px solid var(--accent-maroon);
        }

        /* Input Area */
        .input-area {
            border-top: 1px solid var(--border-light);
            background: var(--bg-primary);
            padding: 20px 0 32px;
        }

        .input-wrapper {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 24px;
        }

        .input-container {
            background: var(--bg-primary);
            border: 1px solid var(--border-light);
            border-radius: 12px;
            padding: 12px 16px;
            display: flex;
            align-items: flex-end;
            gap: 12px;
            transition: all 0.2s ease;
            box-shadow: var(--shadow-subtle);
        }

        .input-container:focus-within {
            border-color: var(--accent-maroon);
            box-shadow: 0 0 0 3px rgba(148, 0, 50, 0.1);
        }

        .input-textarea {
            flex: 1;
            border: none;
            background: transparent;
            font-size: 15px;
            font-family: inherit;
            color: var(--text-primary);
            outline: none;
            resize: none;
            max-height: 200px;
            line-height: 1.5;
        }

        .input-textarea::placeholder {
            color: var(--text-tertiary);
        }

        .input-actions {
            display: flex;
            gap: 6px;
        }

        .input-icon-btn {
            width: 32px;
            height: 32px;
            border: none;
            background: transparent;
            color: var(--text-tertiary);
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.15s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
        }

        .input-icon-btn:hover {
            background: var(--bg-secondary);
            color: var(--text-secondary);
        }

        .send-btn {
            width: 32px;
            height: 32px;
            background: var(--accent-maroon);
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.15s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
        }

        .send-btn:hover:not(:disabled) {
            background: var(--accent-maroon-hover);
        }

        .send-btn:disabled {
            background: var(--bg-tertiary);
            cursor: not-allowed;
            opacity: 0.5;
        }

        /* Loading */
        .loading-indicator {
            display: flex;
            gap: 6px;
            padding: 8px 0;
        }

        .loading-dot {
            width: 8px;
            height: 8px;
            background: var(--text-tertiary);
            border-radius: 50%;
            animation: bounce 1.4s infinite ease-in-out both;
        }

        .loading-dot:nth-child(1) { animation-delay: -0.32s; }
        .loading-dot:nth-child(2) { animation-delay: -0.16s; }

        @keyframes bounce {
            0%, 80%, 100% { transform: scale(0.8); opacity: 0.5; }
            40% { transform: scale(1); opacity: 1; }
        }

        /* Welcome Screen */
        .welcome-screen {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%;
            padding: 48px 32px;
            text-align: center;
        }

        .welcome-logo-wrapper {
            width: 200px;
            height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 40px;
            animation: fadeInScale 0.6s ease-out;
        }

        .welcome-logo {
            width: 200px;
            height: 200px;
            object-fit: contain;
        }

        @keyframes fadeInScale {
            from {
                opacity: 0;
                transform: scale(0.9) translateY(20px);
            }
            to {
                opacity: 1;
                transform: scale(1) translateY(0);
            }
        }

        .welcome-title {
            font-size: 36px;
            font-weight: 600;
            color: var(--accent-maroon);
            margin-bottom: 16px;
            letter-spacing: -0.02em;
        }

        .welcome-subtitle {
            font-size: 16px;
            color: var(--text-secondary);
            margin-bottom: 32px;
            max-width: 600px;
            line-height: 1.6;
        }

        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 12px;
            max-width: 800px;
            width: 100%;
        }

        .quick-action-card {
            padding: 16px 20px;
            background: var(--bg-secondary);
            border: 1px solid var(--border-light);
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.2s ease;
            text-align: left;
            display: flex;
            align-items: flex-start;
            gap: 12px;
        }

        .quick-action-card:hover {
            background: var(--bg-tertiary);
            border-color: var(--accent-maroon);
            transform: translateY(-2px);
        }

        .quick-action-icon {
            width: 20px;
            height: 20px;
            flex-shrink: 0;
            opacity: 0.6;
        }

        .quick-action-content {
            flex: 1;
        }

        .quick-action-title {
            font-size: 14px;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 4px;
        }

        .quick-action-desc {
            font-size: 13px;
            color: var(--text-secondary);
        }

        /* Scrollbar */
        .messages-container::-webkit-scrollbar,
        .sidebar-nav::-webkit-scrollbar {
            width: 6px;
        }

        .messages-container::-webkit-scrollbar-track,
        .sidebar-nav::-webkit-scrollbar-track {
            background: transparent;
        }

        .messages-container::-webkit-scrollbar-thumb,
        .sidebar-nav::-webkit-scrollbar-thumb {
            background: var(--border-light);
            border-radius: 3px;
        }

        .messages-container::-webkit-scrollbar-thumb:hover,
        .sidebar-nav::-webkit-scrollbar-thumb:hover {
            background: var(--text-tertiary);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                position: fixed;
                left: -260px;
                top: 0;
                height: 100vh;
                z-index: 100;
                transition: left 0.3s ease;
                box-shadow: var(--shadow-soft);
            }

            .sidebar.open {
                left: 0;
            }

            .messages-wrapper,
            .input-wrapper {
                padding: 24px 16px;
            }

            .quick-actions {
                grid-template-columns: 1fr;
            }
        }

        /* Utility */
        .hidden {
            display: none;
        }
    </style>
</head>
<body>

        /* Top Header Bar */
        .header-bar {
            grid-column: 1 / -1;
            background: var(--primary-maroon);
            color: var(--white);
            display: flex;
            align-items: center;
            padding: 0 calc(var(--grid-unit) * 3);
            box-shadow: var(--shadow-md);
            z-index: 100;
        }

        .header-logo {
            display: flex;
            align-items: center;
            gap: calc(var(--grid-unit) * 2);
        }

        .logo-icon {
            width: 48px;
            height: 48px;
            background: var(--white);
            border-radius: calc(var(--grid-unit) * 1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: var(--primary-maroon);
            font-size: 24px;
        }

        .header-title {
            display: flex;
            flex-direction: column;
        }

        .header-title h1 {
            font-size: 18px;
            font-weight: 600;
            letter-spacing: -0.02em;
        }

        .header-title p {
            font-size: 13px;
            font-weight: 400;
            opacity: 0.9;
            margin-top: 2px;
        }

        .header-actions {
            margin-left: auto;
            display: flex;
            gap: calc(var(--grid-unit) * 2);
            align-items: center;
        }

        .header-btn {
            background: rgba(255, 255, 255, 0.15);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: var(--white);
            padding: calc(var(--grid-unit) * 1) calc(var(--grid-unit) * 2);
            border-radius: calc(var(--grid-unit) * 1);
            font-size: 14px;
            cursor: pointer;
            transition: all 0.2s;
            font-weight: 500;
        }

        .header-btn:hover {
            background: rgba(255, 255, 255, 0.25);
        }

        /* Sidebar Navigation */
        .sidebar {
            background: var(--white);
            border-right: 1px solid var(--medium-gray);
            padding: calc(var(--grid-unit) * 3);
            overflow-y: auto;
        }

        .sidebar-section {
            margin-bottom: calc(var(--grid-unit) * 4);
        }

        .sidebar-section h3 {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: calc(var(--grid-unit) * 2);
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: calc(var(--grid-unit) * 1.5);
            padding: calc(var(--grid-unit) * 1.5) calc(var(--grid-unit) * 2);
            border-radius: calc(var(--grid-unit) * 1);
            cursor: pointer;
            transition: all 0.2s;
            color: var(--text-primary);
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: calc(var(--grid-unit) * 1);
        }

        .nav-item:hover {
            background: var(--light-gray);
        }

        .nav-item.active {
            background: rgba(148, 0, 50, 0.1);
            color: var(--primary-maroon);
        }

        .nav-icon {
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .quick-action-btn {
            width: 100%;
            background: var(--primary-maroon);
            color: var(--white);
            border: none;
            padding: calc(var(--grid-unit) * 1.5);
            border-radius: calc(var(--grid-unit) * 1);
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            margin-top: calc(var(--grid-unit) * 2);
        }

        .quick-action-btn:hover {
            background: var(--primary-dark);
        }

        /* Main Chat Panel */
        .chat-panel {
            display: flex;
            flex-direction: column;
            background: var(--light-gray);
            height: calc(100vh - 72px);
        }

        .chat-subheader {
            background: var(--white);
            border-bottom: 1px solid var(--medium-gray);
            padding: calc(var(--grid-unit) * 2) calc(var(--grid-unit) * 3);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .chat-subheader h2 {
            font-size: 16px;
            font-weight: 600;
            color: var(--text-primary);
        }

        .chat-status {
            display: flex;
            align-items: center;
            gap: calc(var(--grid-unit) * 1);
            font-size: 13px;
            color: var(--text-secondary);
        }

        .status-indicator {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: #4caf50;
        }

        /* Messages Area */
        .chat-messages {
            flex: 1;
            padding: calc(var(--grid-unit) * 3);
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        .message {
            display: flex;
            margin-bottom: calc(var(--grid-unit) * 3);
            animation: fadeInUp 0.3s ease-out;
        }

        @keyframes fadeInUp {
            from { 
                opacity: 0; 
                transform: translateY(12px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        .message.bot {
            justify-content: flex-start;
        }

        .message.user {
            justify-content: flex-end;
        }

        .message-avatar {
            width: 40px;
            height: 40px;
            border-radius: calc(var(--grid-unit) * 1);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 16px;
            flex-shrink: 0;
            margin-right: calc(var(--grid-unit) * 1.5);
        }

        .message.bot .message-avatar {
            background: var(--primary-maroon);
            color: var(--white);
        }

        .message.user .message-avatar {
            background: var(--medium-gray);
            color: var(--text-primary);
            order: 2;
            margin-right: 0;
            margin-left: calc(var(--grid-unit) * 1.5);
        }

        .message-content {
            max-width: 65%;
            display: flex;
            flex-direction: column;
        }

        .message-bubble {
            padding: calc(var(--grid-unit) * 2) calc(var(--grid-unit) * 2.5);
            border-radius: calc(var(--grid-unit) * 1.5);
            box-shadow: var(--shadow-sm);
            word-wrap: break-word;
            line-height: 1.5;
        }

        .message.bot .message-bubble {
            background: var(--white);
            color: var(--text-primary);
            border-bottom-left-radius: calc(var(--grid-unit) * 0.5);
        }

        .message.user .message-bubble {
            background: var(--primary-maroon);
            color: var(--white);
            border-bottom-right-radius: calc(var(--grid-unit) * 0.5);
        }

        .message-meta {
            display: flex;
            align-items: center;
            gap: calc(var(--grid-unit) * 1.5);
            margin-top: calc(var(--grid-unit) * 1);
            font-size: 12px;
            color: var(--text-secondary);
        }

        .confidence-badge {
            display: inline-flex;
            align-items: center;
            gap: calc(var(--grid-unit) * 0.5);
            background: rgba(76, 175, 80, 0.15);
            color: #2e7d32;
            padding: calc(var(--grid-unit) * 0.5) calc(var(--grid-unit) * 1);
            border-radius: calc(var(--grid-unit) * 0.5);
            font-size: 11px;
            font-weight: 600;
        }

        .confidence-badge.low {
            background: rgba(255, 152, 0, 0.15);
            color: #e65100;
        }

        .sources {
            margin-top: calc(var(--grid-unit) * 1.5);
            padding: calc(var(--grid-unit) * 1.5);
            background: rgba(148, 0, 50, 0.05);
            border-left: 3px solid var(--primary-maroon);
            border-radius: calc(var(--grid-unit) * 0.5);
            font-size: 12px;
            color: var(--text-secondary);
        }

        /* Input Area */
        .chat-input-area {
            background: var(--white);
            border-top: 1px solid var(--medium-gray);
            padding: calc(var(--grid-unit) * 3);
        }

        .input-container {
            display: flex;
            align-items: flex-end;
            gap: calc(var(--grid-unit) * 2);
            background: var(--light-gray);
            border: 2px solid var(--medium-gray);
            border-radius: calc(var(--grid-unit) * 1.5);
            padding: calc(var(--grid-unit) * 1.5);
            transition: all 0.2s;
        }

        .input-container:focus-within {
            border-color: var(--primary-maroon);
            background: var(--white);
        }

        .input-tools {
            display: flex;
            gap: calc(var(--grid-unit) * 1);
        }

        .input-icon-btn {
            width: 36px;
            height: 36px;
            border: none;
            background: transparent;
            color: var(--dark-gray);
            border-radius: calc(var(--grid-unit) * 1);
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .input-icon-btn:hover {
            background: var(--medium-gray);
            color: var(--primary-maroon);
        }

        .chat-input {
            flex: 1;
            border: none;
            background: transparent;
            font-size: 15px;
            font-family: inherit;
            color: var(--text-primary);
            outline: none;
            resize: none;
            max-height: 120px;
            line-height: 1.5;
        }

        .chat-input::placeholder {
            color: var(--dark-gray);
        }

        .send-button {
            width: 40px;
            height: 40px;
            background: var(--primary-maroon);
            color: var(--white);
            border: none;
            border-radius: calc(var(--grid-unit) * 1);
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }

        .send-button:hover:not(:disabled) {
            background: var(--primary-dark);
            transform: scale(1.05);
        }

        .send-button:disabled {
            background: var(--medium-gray);
            cursor: not-allowed;
        }

        /* Loading Animation */
        .loading {
            display: flex;
            gap: calc(var(--grid-unit) * 1);
            padding: calc(var(--grid-unit) * 1);
        }

        .loading-dot {
            width: 8px;
            height: 8px;
            background: var(--primary-maroon);
            border-radius: 50%;
            animation: bounce 1.4s infinite ease-in-out both;
        }

        .loading-dot:nth-child(1) { animation-delay: -0.32s; }
        .loading-dot:nth-child(2) { animation-delay: -0.16s; }

        @keyframes bounce {
            0%, 80%, 100% { transform: scale(0); opacity: 0.3; }
            40% { transform: scale(1); opacity: 1; }
        }

        /* Scrollbar */
        .chat-messages::-webkit-scrollbar,
        .sidebar::-webkit-scrollbar {
            width: 6px;
        }

        .chat-messages::-webkit-scrollbar-track,
        .sidebar::-webkit-scrollbar-track {
            background: transparent;
        }

        .chat-messages::-webkit-scrollbar-thumb,
        .sidebar::-webkit-scrollbar-thumb {
            background: var(--medium-gray);
            border-radius: 3px;
        }

        .chat-messages::-webkit-scrollbar-thumb:hover,
        .sidebar::-webkit-scrollbar-thumb:hover {
            background: var(--dark-gray);
        }

        /* Responsive Design */
        @media (max-width: 968px) {
            .app-container {
                grid-template-columns: 1fr;
            }

            .sidebar {
                display: none;
            }

            .mobile-menu-toggle {
                display: block;
            }

            .message-content {
                max-width: 85%;
            }
        }

        @media (max-width: 640px) {
            .header-title p {
                display: none;
            }

            .header-actions {
                gap: calc(var(--grid-unit) * 1);
            }

            .chat-messages {
                padding: calc(var(--grid-unit) * 2);
            }

            .message-avatar {
                width: 32px;
                height: 32px;
                font-size: 14px;
            }
        }

        /* Utility */
        .hidden {
            display: none;
        }
    </style>
</head>
<body>
    <!-- App Container -->
    <div class="app-container">
        
        <!-- Minimal Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo-container">
                    <img src="../image/rmutp-logo1.png" alt="RMUTP Logo" class="logo-image">
                    <div class="logo-text">
                        <div class="logo-title">คณะวิศวกรรมศาสตร์</div>
                        <div class="logo-subtitle">มทร.พระนคร</div>
                    </div>
                </div>
                
                <button class="new-chat-btn" onclick="newChat()">
                    <svg class="icon-svg" viewBox="0 0 24 24">
                        <path d="M12 5v14M5 12h14"/>
                    </svg>
                    <span>การสนทนาใหม่</span>
                </button>
            </div>

            <nav class="sidebar-nav">
                <div class="nav-section">
                    <div class="nav-section-title">เมนู</div>
                    <a href="#" class="nav-item active">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
                            </svg>
                        </span>
                        <span>แชท</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <circle cx="12" cy="12" r="10"/>
                                <polyline points="12 6 12 12 16 14"/>
                            </svg>
                        </span>
                        <span>ประวัติ</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                            </svg>
                        </span>
                        <span>ยอดนิยม</span>
                    </a>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">หมวดหมู่</div>
                    <a href="#" class="nav-item">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/>
                                <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/>
                            </svg>
                        </span>
                        <span>หลักสูตร</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                                <polyline points="14 2 14 8 20 8"/>
                                <line x1="16" y1="13" x2="8" y2="13"/>
                                <line x1="16" y1="17" x2="8" y2="17"/>
                                <polyline points="10 9 9 9 8 9"/>
                            </svg>
                        </span>
                        <span>รับสมัคร</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                                <circle cx="12" cy="7" r="4"/>
                            </svg>
                        </span>
                        <span>บุคลากร</span>
                    </a>
                    <a href="#" class="nav-item">
                        <span class="nav-icon">
                            <svg class="icon-svg" viewBox="0 0 24 24">
                                <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>
                                <path d="M13.73 21a2 2 0 0 1-3.46 0"/>
                            </svg>
                        </span>
                        <span>ข่าวสาร</span>
                    </a>
                </div>
            </nav>

            <div class="sidebar-footer">
                <a href="#" class="nav-item">
                    <span class="nav-icon">
                        <svg class="icon-svg" viewBox="0 0 24 24">
                            <circle cx="12" cy="12" r="10"/>
                            <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/>
                            <line x1="12" y1="17" x2="12.01" y2="17"/>
                        </svg>
                    </span>
                    <span>ช่วยเหลือ</span>
                </a>
                <a href="#" class="nav-item">
                    <span class="nav-icon">
                        <svg class="icon-svg" viewBox="0 0 24 24">
                            <circle cx="12" cy="12" r="3"/>
                            <path d="M12 1v6m0 6v6"/>
                            <path d="m4.93 4.93 4.24 4.24m5.66 5.66 4.24 4.24"/>
                            <path d="M1 12h6m6 0h6"/>
                            <path d="m4.93 19.07 4.24-4.24m5.66-5.66 4.24-4.24"/>
                        </svg>
                    </span>
                    <span>ตั้งค่า</span>
                </a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Top Bar -->
            <div class="top-bar">
                <div class="top-bar-title">คณะวิศวกรรมศาสตร์ มทร.พระนคร</div>
                <div class="top-bar-actions">
                    <button class="icon-btn" title="ประวัติ">
                        <svg class="icon-svg" viewBox="0 0 24 24">
                            <path d="M9 11H3v8h6v-8zM21 11h-6v8h6v-8zM15 3H9v6h6V3z"/>
                        </svg>
                    </button>
                    <button class="icon-btn" title="ตั้งค่า">
                        <svg class="icon-svg" viewBox="0 0 24 24">
                            <circle cx="12" cy="12" r="3"/>
                            <path d="M12 1v6m0 6v6"/>
                            <path d="m4.93 4.93 4.24 4.24m5.66 5.66 4.24 4.24"/>
                            <path d="M1 12h6m6 0h6"/>
                            <path d="m4.93 19.07 4.24-4.24m5.66-5.66 4.24-4.24"/>
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Messages -->
            <div class="messages-container" id="messagesContainer">
                <div class="messages-wrapper" id="messagesWrapper">
                    <!-- Welcome Screen (hidden after first message) -->
                    <div class="welcome-screen" id="welcomeScreen">
                        <div class="welcome-logo-wrapper">
                            <img src="../image/rmutp-logo1.png" alt="RMUTP Logo" class="welcome-logo">
                        </div>
                        <h1 class="welcome-title">สวัสดีครับ</h1>
                        <p class="welcome-subtitle">
                            ยินดีต้อนรับสู่ระบบแชทบอทคณะวิศวกรรมศาสตร์<br>
                            มหาวิทยาลัยเทคโนโลยีราชมงคลพระนคร<br>
                            พร้อมให้ข้อมูลและตอบคำถามของคุณ
                        </p>
                        
                        <div class="quick-actions">
                            <div class="quick-action-card" onclick="sendQuickMessage('มีหลักสูตรอะไรบ้าง')">
                                <svg class="quick-action-icon" viewBox="0 0 24 24" fill="none" stroke="var(--accent-maroon)" stroke-width="2">
                                    <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/>
                                    <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/>
                                </svg>
                                <div class="quick-action-content">
                                    <div class="quick-action-title">หลักสูตร</div>
                                    <div class="quick-action-desc">ดูข้อมูลหลักสูตรและสาขาวิชา</div>
                                </div>
                            </div>
                            <div class="quick-action-card" onclick="sendQuickMessage('วิธีสมัครเรียน')">
                                <svg class="quick-action-icon" viewBox="0 0 24 24" fill="none" stroke="var(--accent-maroon)" stroke-width="2">
                                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                                    <polyline points="14 2 14 8 20 8"/>
                                </svg>
                                <div class="quick-action-content">
                                    <div class="quick-action-title">การรับสมัคร</div>
                                    <div class="quick-action-desc">ขั้นตอนการสมัครและคุณสมบัติ</div>
                                </div>
                            </div>
                            <div class="quick-action-card" onclick="sendQuickMessage('ติดต่ออาจารย์')">
                                <svg class="quick-action-icon" viewBox="0 0 24 24" fill="none" stroke="var(--accent-maroon)" stroke-width="2">
                                    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                                    <circle cx="12" cy="7" r="4"/>
                                </svg>
                                <div class="quick-action-content">
                                    <div class="quick-action-title">บุคลากร</div>
                                    <div class="quick-action-desc">ข้อมูลอาจารย์และเจ้าหน้าที่</div>
                                </div>
                            </div>
                            <div class="quick-action-card" onclick="sendQuickMessage('มีทุนการศึกษาไหม')">
                                <svg class="quick-action-icon" viewBox="0 0 24 24" fill="none" stroke="var(--accent-gold)" stroke-width="2">
                                    <line x1="12" y1="1" x2="12" y2="23"/>
                                    <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
                                </svg>
                                <div class="quick-action-content">
                                    <div class="quick-action-title">ทุนการศึกษา</div>
                                    <div class="quick-action-desc">ข้อมูลทุนและการสนับสนุน</div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Input Area -->
            <div class="input-area">
                <div class="input-wrapper">
                    <div class="input-container">
                        <div class="input-actions">
                            <button class="input-icon-btn" title="แนบไฟล์" onclick="attachFile()">
                                <svg class="icon-svg" viewBox="0 0 24 24">
                                    <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48"/>
                                </svg>
                            </button>
                        </div>
                        <textarea 
                            class="input-textarea" 
                            id="userInput" 
                            rows="1"
                            placeholder="พิมพ์คำถามของคุณ..."
                        ></textarea>
                        <button class="send-btn" id="sendBtn" onclick="sendMessage()" title="ส่ง">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 18px; height: 18px;">
                                <line x1="22" y1="2" x2="11" y2="13"/>
                                <polygon points="22 2 15 22 11 13 2 9 22 2"/>
                            </svg>
                        </button>
                    </div>
                </div>
            </div>
        </main>
    </div>


    <script>
        const API_URL = '../backend/chatbot.php';
        const SESSION_ID = 'sess_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
        
        const messagesContainer = document.getElementById('messagesContainer');
        const messagesWrapper = document.getElementById('messagesWrapper');
        const welcomeScreen = document.getElementById('welcomeScreen');
        const userInput = document.getElementById('userInput');
        const sendBtn = document.getElementById('sendBtn');

        let hasMessages = false;

        // Auto-resize textarea
        userInput.addEventListener('input', function() {
            this.style.height = 'auto';
            this.style.height = Math.min(this.scrollHeight, 200) + 'px';
        });

        // Enter to send (Shift+Enter for new line)
        userInput.addEventListener('keydown', (e) => {
            if (e.key === 'Enter' && !e.shiftKey) {
                e.preventDefault();
                sendMessage();
            }
        });

        function newChat() {
            location.reload();
        }

        function sendQuickMessage(message) {
            userInput.value = message;
            sendMessage();
        }

        async function sendMessage() {
            const message = userInput.value.trim();
            
            if (!message) return;
            
            // Hide welcome screen on first message
            if (!hasMessages) {
                welcomeScreen.style.display = 'none';
                hasMessages = true;
            }
            
            // Disable input
            userInput.disabled = true;
            sendBtn.disabled = true;
            
            // Add user message
            addMessage('user', message);
            userInput.value = '';
            userInput.style.height = 'auto';
            
            // Show loading
            const loadingId = addLoadingMessage();
            
            try {
                const response = await fetch(API_URL, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json; charset=utf-8',
                    },
                    body: JSON.stringify({
                        session_id: SESSION_ID,
                        message: message
                    })
                });
                
                const data = await response.json();
                
                removeLoadingMessage(loadingId);
                
                if (data.error) {
                    addMessage('bot', '⚠️ ' + data.error);
                } else {
                    addMessage('bot', data.answer, data.confidence, data.sources, data.response_time_ms);
                }
                
            } catch (error) {
                removeLoadingMessage(loadingId);
                addMessage('bot', '⚠️ ไม่สามารถเชื่อมต่อกับระบบได้ กรุณาลองใหม่อีกครั้ง');
            }
            
            userInput.disabled = false;
            sendBtn.disabled = false;
            userInput.focus();
        }

        function addMessage(type, text, confidence = null, sources = null, responseTime = null) {
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${type}`;
            
            const time = new Date().toLocaleTimeString('th-TH', { hour: '2-digit', minute: '2-digit' });
            
            // Avatar SVG
            const botAvatar = `
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="3" y="11" width="18" height="10" rx="2"/>
                    <circle cx="12" cy="5" r="2"/>
                    <path d="M12 7v4"/>
                    <line x1="8" y1="16" x2="8" y2="16"/>
                    <line x1="16" y1="16" x2="16" y2="16"/>
                </svg>
            `;
            
            const userAvatar = `
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                    <circle cx="12" cy="7" r="4"/>
                </svg>
            `;
            
            let html = `
                <div class="message-avatar">${type === 'bot' ? botAvatar : userAvatar}</div>
                <div class="message-content">
                    <div class="message-text">${text}</div>
            `;
            
            // Meta info
            if (type === 'bot') {
                html += `<div class="message-meta">`;
                html += `<span>${time}</span>`;
                
                if (confidence !== null) {
                    const confClass = confidence >= 0.7 ? '' : 'low';
                    const confPercent = (confidence * 100).toFixed(0);
                    html += `<span class="confidence-badge ${confClass}">แม่นยำ ${confPercent}%</span>`;
                }
                
                if (responseTime !== null) {
                    html += `<span>${responseTime}ms</span>`;
                }
                
                html += `</div>`;
                
                // Sources
                if (sources && sources.length > 0) {
                    html += `<div class="sources-info">
                        <svg style="width: 14px; height: 14px; display: inline-block; vertical-align: middle; margin-right: 4px;" viewBox="0 0 24 24" fill="none" stroke="var(--accent-maroon)" stroke-width="2">
                            <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/>
                            <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/>
                        </svg>
                        อ้างอิงจาก: `;
                    sources.forEach((src, i) => {
                        html += `${src.type} #${src.id}`;
                        if (i < sources.length - 1) html += ', ';
                    });
                    html += `</div>`;
                }
            }
            
            html += `</div>`;
            messageDiv.innerHTML = html;
            
            messagesWrapper.appendChild(messageDiv);
            scrollToBottom();
        }

        function addLoadingMessage() {
            const loadingId = 'loading_' + Date.now();
            const messageDiv = document.createElement('div');
            messageDiv.className = 'message bot';
            messageDiv.id = loadingId;
            messageDiv.innerHTML = `
                <div class="message-avatar">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <rect x="3" y="11" width="18" height="10" rx="2"/>
                        <circle cx="12" cy="5" r="2"/>
                        <path d="M12 7v4"/>
                        <line x1="8" y1="16" x2="8" y2="16"/>
                        <line x1="16" y1="16" x2="16" y2="16"/>
                    </svg>
                </div>
                <div class="message-content">
                    <div class="loading-indicator">
                        <div class="loading-dot"></div>
                        <div class="loading-dot"></div>
                        <div class="loading-dot"></div>
                    </div>
                </div>
            `;
            messagesWrapper.appendChild(messageDiv);
            scrollToBottom();
            return loadingId;
        }

        function removeLoadingMessage(loadingId) {
            const elem = document.getElementById(loadingId);
            if (elem) elem.remove();
        }

        function scrollToBottom() {
            messagesContainer.scrollTop = messagesContainer.scrollHeight;
        }

        function attachFile() {
            alert('ฟีเจอร์แนบไฟล์จะพร้อมใช้งานเร็วๆ นี้');
        }

        // Focus input on load
        window.addEventListener('load', () => {
            userInput.focus();
        });
    </script>
</body>
</html>
</body>
</html>
</body>
</html>
